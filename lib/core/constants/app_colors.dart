import 'package:flutter/material.dart';

/// Centralized color definitions for the app
class AppColors {
  // ------------------------
  // Branding & Theme Colors
  // ------------------------
  static const Color primaryAppColor = Color(0xFF00C853); // Vibrant green
  static const Color secondaryAppColor = Color(0xFF2979FF); // Blue for secondary
  static const Color accentColor = Color(0xFF00B8D4); // Teal for accents

  // ------------------------
  // Background & Whites
  // ------------------------
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color background = Color(0xFFFAFAFA); // Slightly off-white for background

  // ------------------------
  // Overlays & Indicators
  // ------------------------
  static const Color darkOverlay = Color(0x99000000); // 60% black overlay
  static const Color indicatorTrackColor = Colors.white54;
  static const Color indicatorProgressColor = Colors.black;

  // ------------------------
  // Typography & Text Colors
  // ------------------------
  static const Color primaryTextColor = Color(0xFF212121); // Dark gray for text
  static const Color secondaryTextColor = Color(0xFF757575); // Medium gray
  static const Color hintColor = Color(0xFFBDBDBD);
  static const Color disabledTextColor = Color(0xFFBDBDBD);

  // ------------------------
  // UI & Widget Colors
  // ------------------------
  static const Color borderColor = Color(0xFFE0E0E0);
  static const Color googleBlue = Color(0xFF4285F4);
  static const Color facebookBlue = Color(0xFF3B5998);

  // ------------------------
  // Error & Success Colors
  // ------------------------
  static const Color errorRed = Color(0xFFD32F2F);
  static const Color successGreen = Color(0xFF388E3C);

  // ------------------------
  // Other Colors
  // ------------------------
  static const Color searchBackground = Color(0xFFF2F3F2);
}